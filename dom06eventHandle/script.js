// * EventTarget.addEventListener

// * EventTarget.removeEventListener , elem.onclick = null

//* type, target（点击对象）, currentTarget(监听对象)

// *stopPropagation(prevent bubble), preventDefault, stopimmediatePropagation

//
